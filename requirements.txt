streamlit>=1.28.0
pandas>=2.0.0
python-dateutil>=2.8.0

# Optional: LLM dependencies for natural language generation
# The application works without these (uses deterministic fallback)
# Uncomment the provider you want to use:
# openai>=1.0.0              # For OpenAI GPT models
# google-generativeai>=0.3.0 # For Google Gemini
# anthropic>=0.7.0           # For Anthropic Claude
